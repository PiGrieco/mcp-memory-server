# === Minimal config (copia in .env) ===
AUTO_TRIGGER_MODEL=PiGrieco/mcp-memory-auto-trigger-model
HF_HOME=/models
MODEL_DEVICE=cpu

# Abilita persistenza e search quando sei pronto:
# MONGO_URI=mongodb://localhost:27017
# MONGO_DB=mcp_memory
# EMBEDDINGS_PROVIDER=sentence-transformers/all-MiniLM-L6-v2
# API_KEY=  # se vuoi proteggere l'HTTP API
