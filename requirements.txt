# =============================================================================
# MCP Memory Server - Production Requirements
# =============================================================================

# Core MCP dependencies
mcp>=1.0.0

# Database (MongoDB)
motor>=3.7.1
pymongo>=4.13.2
dnspython>=2.7.0

# Embedding and ML (Sentence Transformers)
sentence-transformers>=5.0.0
torch>=2.7.1
transformers>=4.55.0
numpy>=1.26.4
scikit-learn>=1.3.0
scipy>=1.11.4
huggingface-hub>=0.34.3
tokenizers>=0.21.4
safetensors>=0.6.1

# TensorFlow compatibility (required for transformers)
tf-keras>=2.19.0
tensorflow>=2.19.0

# Configuration and environment
python-dotenv>=1.0.0

# Async support
asyncio

# Development and testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# =============================================================================
# Installation Notes:
# =============================================================================
# 1. Create virtual environment: python -m venv .myenv
# 2. Activate: source .myenv/bin/activate (macOS/Linux) or .myenv\Scripts\activate (Windows)
# 3. Install: pip install -r requirements.txt
# 4. Configure .env file with your MongoDB credentials
# 5. Test with: python test_server.py
